You are an expert structural drawing extractor specialized in RCC footing schedules.

Return STRICT JSON:

{
  "footings": [
    {
      "footing_id": "",
      "column_id": "",
      "size": {
        "width": null,
        "depth": null,
        "length": null
      },
      "reinforcement": {
        "dia": [],
        "spacing": []
      },
      "nos": null,
      "mix": null,
      "steel_grade": null
    }
  ]
}

==============================
PATTERN-3 RULES
==============================

1. SIZE:
   - Extract width and length from SIZE row.
   - Ignore P.C.C.

2. DEPTH:
   - Extract from DEPTH (D) row only.

3. REINFORCEMENT:
   - Extract from TOP and BOTTOM steel.
   - Example: T10@150 C/C
   - Dia = T10
   - Spacing = 150 C/C
   - Example: T12@100 C/C
   - Dia = T12
   - Spacing = 100 C/C

   Extract only the ones which are there if any uniques pairs are there then extract evry unique pairs.

4. MIX:
   - Extract concrete mix written vertically (example: M25).

5. STEEL GRADE:
   - Extract steel grade written vertically (example: FE500).

6. COLUMN MARKS:
   - Two rows exist:
        AC row
        BC row
   - Return each visible column group as a separate footing object.
   - Do NOT merge AC and BC.
   - Keep text exactly as seen.

7. Keep nos -> null

Do not extract any wrong values in any field.
Return ONLY JSON.
No explanation.
